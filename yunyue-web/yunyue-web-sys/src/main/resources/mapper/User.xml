<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.yunyue.sys.user.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="user" extends="common.resultMap">
        <result column="CODE" jdbcType="VARCHAR" property="code"/>
        <result column="DEPT_ID" jdbcType="VARCHAR" property="deptId"/>
        <result column="TEL" jdbcType="VARCHAR" property="tel"/>
        <result column="EMAIL" jdbcType="VARCHAR" property="email"/>
        <result column="ACCOUNT" jdbcType="VARCHAR" property="account"/>
        <result column="PASSWORD" jdbcType="VARCHAR" property="password"/>
        <result column="SALT" jdbcType="VARCHAR" property="salt"/>
        <result column="AVATAR" jdbcType="VARCHAR" property="avatar"/>
        <result column="NAME" jdbcType="VARCHAR" property="name"/>
        <result column="NICK_NAME" jdbcType="VARCHAR" property="nickName"/>
        <result column="REGISTER_IP" jdbcType="VARCHAR" property="registerIp"/>
        <result column="GMT_REGISTER" jdbcType="TIMESTAMP" property="gmtRegister"/>
        <result column="LAST_IP" jdbcType="VARCHAR" property="lastIp"/>
        <result column="LAST_LOGIN" jdbcType="TIMESTAMP" property="lastLogin"/>
        <result column="LOCKED" jdbcType="TINYINT" property="locked"/>
        <result column="DEPT_NAME" jdbcType="TINYINT" property="deptName"/>
        <result column="POST_ID" jdbcType="VARCHAR" property="postId"/>
        <result column="U_POST_ID" jdbcType="VARCHAR" property="uPostId"/>
        <result column="POST_NAME" jdbcType="VARCHAR" property="postName"/>
    </resultMap>
    <!-- 获取包含权限列表的用户信息-->
    <resultMap id="UserWithRole" type="user" extends="BaseResultMap">
        <collection property="roles" column="{userId=id}" select="tech.yunyue.sys.role.mapper.RoleMapper.selectByUserId"
                    ofType="role"/>
    </resultMap>

    <!-- 获取包含权限列表的用户信息-->
    <resultMap id="UserWithRoleId" type="user" extends="BaseResultMap">
        <collection property="roles" javaType="java.util.List" ofType="roleUser">
            <id column="R_ID" property="id" jdbcType="VARCHAR"/>
            <result column="ROLE_ID" property="roleId" jdbcType="VARCHAR"/>
        </collection>
    </resultMap>

    <!-- 获取包含模块 权限列表的用户信息-->
    <resultMap id="UserWithModuleAndRole" type="user" extends="BaseResultMap">
        <collection property="roles" column="{userId=id}" select="tech.yunyue.sys.role.mapper.RoleMapper.selectByUserId"
                    ofType="role"/>
        <collection property="modules" column="{userId=id}"
                    select="tech.yunyue.sys.module.mapper.ModuleMapper.getRoleModuleByUserId" ofType="module"/>
    </resultMap>

    <select id="getUerWithRoleId" resultMap="UserWithRoleId">
        SELECT
        su.*,
        sru.id R_ID,
        sru.ROLE_ID
        FROM
        sys_user_info su
        LEFT JOIN sys_role_user sru
        ON su.id = sru.USER_ID
        <if test="null != user.deptId and user.deptId != ''">
--             返回部门和其子部门的信息 并且根据父子关系排名
            RIGHT JOIN (SELECT id,@rank := @rank+1 rankNum FROM ( SELECT #{user.deptId,jdbcType=VARCHAR} id UNION (SELECT id FROM sys_organization WHERE DELETED = '0000' AND LOCATE(CONCAT(#{user.deptId,jdbcType=VARCHAR},'/'), path) > 0 ORDER BY path)) id_r,(select @rank:=0) r) so
            on su.dept_id = so.id
        </if>
        <where>
            <if test="null != user.account and user.account != ''">
                and su.account like concat('%', #{user.account,jdbcType=VARCHAR},'%')
            </if>
            <if test="null != user.name and user.name != ''">
                and su.name like concat('%', #{user.name,jdbcType=VARCHAR},'%')
            </if>
            <if test="null != user.tel and user.tel != ''">
                and su.tel like concat('%', #{user.tel,jdbcType=VARCHAR},'%')
            </if>
            and su.deleted = '0000'
        </where>
        <if test="null != user.deptId and user.deptId != ''">
--         根据部门排名 当前查询的部门用户排在前面
            order by so.rankNum
        </if>
    </select>
    <!-- 根据用户 账号/电话/邮箱 获取包含权限列表的用户信息 -->
    <select id="getUserWithRole" resultMap="UserWithRole">
        select * from sys_user_info su
        <where>
            <if test="null != id and id != ''">
                su.id = #{id,jdbcType=VARCHAR}
            </if>
            <if test="null != account and account != ''">
                and (su.account = #{account,jdbcType=VARCHAR} or
                su.tel = #{account,jdbcType=VARCHAR} or
                su.email = #{account,jdbcType=VARCHAR} )
            </if>
            and su.deleted = '0000'
        </where>
    </select>
    <!-- 根据用户ID 获取包含模块 权限列表的用户信息-->
    <select id="getUserWithModuleAndRoleById" resultMap="UserWithModuleAndRole">
        select *
        from sys_user_info
        where id = #{userId,jdbcType=VARCHAR} and deleted = '0000'
    </select>
	<!-- 根据用户id查询用户角色 -->
    <select id="getRoleById" resultType="java.lang.String">
        select r.`CODE`
        from sys_user_info u
            LEFT JOIN sys_role_user ru on u.id = ru.USER_ID
            LEFT JOIN sys_role r on ru.ROLE_ID = r.ID
        where u.id=#{userId,jdbcType=VARCHAR} and u.DELETED ='0000';
    </select>
</mapper>
