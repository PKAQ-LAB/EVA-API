apply from: '../publish.gradle'
project.archivesBaseName = 'addon-weixin'

dependencies {
    implementation project(":eva-core")

        // 微信公众号
    api "${lib.wxmp}",
        // 微信支付
        "${lib.wxpay}"
}
