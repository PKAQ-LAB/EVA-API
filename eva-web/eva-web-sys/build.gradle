apply from: '../../publish.gradle'
project.archivesBaseName = 'eva-web-sys'
project.description='sys jar with authority and system management'

dependencies {
    implementation("${lib.web}"){
        exclude module: 'spring-boot-starter-tomcat'
    }

    implementation "${lib.undertow}"

    implementation	project(":eva-web:eva-web-base"),
                    project(":eva-core:eva-core-security")
}
