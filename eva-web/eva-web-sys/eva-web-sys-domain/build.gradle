if (project.file('../../../publish.gradle').exists()) {
    apply from: '../../../publish.gradle'
}


project.description = 'domain jar for authority and system management'


jar {
    archiveBaseName = 'eva-web-sys-domain'
}

dependencies {
    api project(":eva-core:eva-core-contract")

    compileOnly libs.json,
                libs.springdoc,
                libs.validation
}
