project.archivesBaseName = 'extra-generator'

project.tasks.publish.enabled = false

jar {
	enabled = true
}

description = """generator"""
// 依赖配置
dependencies {
	implementation  project(":eva-core:eva-core-common"),
					"${lib.velocity}",
					"${lib.mybatis_plus_generator}"
}
