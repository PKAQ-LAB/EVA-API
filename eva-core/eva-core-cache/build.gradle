if (project.file('../../publish.gradle').exists()) {
    apply from: '../../publish.gradle'
}
project.archivesBaseName = 'eva-core-cache'
project.description='eva cache , redis && caffeine'

dependencies {
    implementation project(":eva-core:eva-core-common")

    api             "${lib.cache}",
                    "${lib.redis}",
                    "${lib.caffeine}"
}
