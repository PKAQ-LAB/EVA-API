apply from: '../../publish.gradle'
project.archivesBaseName = 'eva-core-security'
project.description='eva security support'

dependencies {

	implementation("${lib.web}"){
		exclude module: 'spring-boot-starter-tomcat'
	}

	implementation     project(":eva-core:eva-core-cache"),
					   project(":eva-core:eva-core-common"),
					   "${lib.undertow}"

	api				   "${lib.security}"

}
