if (project.file('../../publish.gradle').exists()) {
    apply from: '../../publish.gradle'
}
project.archivesBaseName = 'eva-core-helper'
project.description='EVA工具帮助包'

dependencies {
    implementation  project(":eva-core:eva-core-cache")
}
