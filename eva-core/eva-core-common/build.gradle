if (project.file('../../publish.gradle').exists()) {
    apply from: '../../publish.gradle'
}

jar {
    archiveBaseName = 'eva-core-common'
}

project.description='eva common class'

dependencies {
    api project(":eva-core:eva-core-contract")

    api libs.aop,
        libs.jwt,
        libs.json,
        libs.springdoc,
        libs.validation
}
