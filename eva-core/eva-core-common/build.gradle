apply from: '../../publish.gradle'
project.archivesBaseName = 'eva-core-common'
project.description='eva common class'

dependencies {
    implementation 	"${lib.p6spy}",
                    "${lib.flyway}",
                    "${lib.mysql}",
                    "${lib.jaxb_api}",
                    "${lib.jasypt}"

    api "${lib.hutool}",
        "${lib.aop}",
        "${lib.jdbc}",
        "${lib.knife4j}",
        "${lib.jwt}",
        "${lib.json}",
        "${lib.validation}"
}
