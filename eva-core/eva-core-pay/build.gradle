apply from: '../../publish.gradle'
project.archivesBaseName = 'extra-pay'

dependencies {
    implementation("${lib.web}"){
        exclude module: 'spring-boot-starter-tomcat'
    }

    implementation "${lib.undertow}"

    implementation	project(":eva-core:eva-core-common"),
                    // 阿里支付
                    "${lib.alipay}",
					"${lib.zxing}"
}
