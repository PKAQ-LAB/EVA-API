apply plugin: "org.springframework.boot"

jar {
	enabled = true
}

bootJar {
	mainClassName = 'io.nerv.JXCBooter'
}

description = """jxc"""
// 依赖配置
dependencies {
    implementation "${lib.eva_core}"
}
